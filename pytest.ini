[pytest]
asyncio_mode = auto
norecursedirs = vendor backups .venv .git data reports tmp logs .cache .playwright-mcp scripts/research
markers =
    slow: opt-in tests that hit live external services (Finviz, yfinance, Alpaca). Run with `pytest -m slow`.
