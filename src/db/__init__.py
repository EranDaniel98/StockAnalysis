"""Database layer — SQLAlchemy 2.0 + Alembic + async session manager.

Concrete implementations of the repository Protocols defined in
src/contracts/protocols/repositories.py live in src/db/repositories/.
"""
