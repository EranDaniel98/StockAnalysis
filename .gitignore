__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
data/cache.db
data/*.db
data/*.html
data/*.json
# Phase 0 Stream D — Parquet OHLCV store (generated from migration script)
data/ohlcv/
# Phase 4 — trained model artifacts (joblib + manifest); registry row is the source of truth
data/models/
# Phase 6 — backup snapshots (timestamped pg_dump + tarballs); never under version control
backups/
# Phase 6 — structlog JSON output
logs/
.venv/
venv/
env/
*.log
.idea/
.vscode/
*.swp
*.swo
# Playwright MCP scratchspace (snapshots, console logs)
.playwright-mcp/
# Editor / migration backups
*.bak
