# Strategy Debate — 2026-05-28

- **Opus model:** `claude-opus-4-7`
- **Gemini model:** `gemini-3.1-pro-preview`
- **Rounds:** 6 · **Opener:** Opus 4.7
- **Format:** symmetric critique over the live `src/factors/*` system

---

### Opus 4.7
# Opening: The measurement problem dwarfs everything else

**The headline claim is statistically indefensible as stated.** A +9.26% α with a ±20-30pp phase-noise envelope and only 2-of-9 phases positive isn't a weak edge — it's *no detected edge at all* at the current sample size. Before debating factor design, we need to confront that no proposed improvement can be validated on this measurement apparatus. Everything downstream is theater until this is fixed.

## Priority 1: Fix the measurement before fixing the strategy

The dossier already names the right tool (phase-averaged stats) but the eval bar is still too low. Concretely:

- **Adopt phase-distribution-as-primary-metric.** Report (median, IQR, %-positive, worst-phase) across all 9 offsets as the *headline*, not a footnote. Kill any reference to offset-0 single-run α in commits, dashboards, briefings.
- **Compute a noise floor via permutation.** Shuffle factor scores within each rebalance date 200x; the resulting α distribution *is* your null. If the real composite's median-phase α doesn't clear the 95th percentile of that null, you have no evidence of edge. This runs on the existing snapshot infrastructure — no new data.
- **Replace `alpha_vs_spy_pct` with Jensen's α + Sharpe + max-DD as a triplet.** The dossier flags this as a known bug; the top-15 β-inflation episode (0.694→0.884 absorbing 70% of "underperformance") proves it actively misleads decisions.

Without this, the value-EPS fix, sizing changes, etc. will all generate offset-0 numbers that look good or bad by luck, and we'll keep flipping configs (top-15 → top-24, etc.) on noise.

## Priority 2: The value-EPS bug is a free win — *because it's a measurement problem too*

Quarterly-vs-YTD mixing in `compute_eps_ttm` doesn't just corrupt the value factor; it contaminates the *composite α attribution*. Fix it not because value will suddenly work, but because we can't honestly assess m/q/v vs m/q until value isn't garbage. Two days of work, removes a known confound.

## Priority 3: The regime gate is solving the wrong problem

The 75-SMA whipsaw cost -6.46% α on a *single rebalance day's* 0.31% miss. This is a **cadence-gate interaction bug**, not a parameter-tuning problem. A 63-day rebalance cannot consume a daily regime signal — you're sampling a fast process at a slow rate. Either:
- evaluate the regime gate **daily** with the portfolio responding (de-risk to cash between rebalances), keeping selection at 63d, or
- accept the gate is decorative and remove it; the COVID -7.9% CAPM-α shows it isn't crash insurance anyway.

Sizing/covariance and universe-freeze matter, but are dominated by these three. Over to Gemini.

### Gemini
Opus, you correctly identify that the ±20-30pp phase-noise envelope renders the headline +9.26% meaningless. But your proposed permutation test will generate a **corrupted null distribution** because you bypassed the dossier's most toxic data leak: the **universe-freeze eligibility bias**.

### 1. Fix the Universe-Freeze Before Trusting Any Metric
If the universe is frozen at the snapshot's `as_of` date rather than resolved point-in-time per rebalance, both the strategy *and* your permutation test are picking from a pool tainted by future knowledge (survivors or known future-eligibles). A permutation test on a biased universe just gives you a biased null. Before we compute Jensen's α or phase-distributions, we must dynamically resolve the S&P 500 constituents at each 63-day step using the existing Polygon/EDGAR data. Until then, *all* measurement is compromised.

### 2. Decouple Regime Gating from Stock Selection
You absolutely nailed the cadence mismatch. Gating a 63-day rebalance on a daily 75-SMA crossing guarantees brutal whipsaws, precisely like the 2025-01-02 miss that cost -6.46% α. 

Here is the concrete, data-compliant fix: **decouple stock selection from market exposure**. Keep the 63-day rebalance for the 24-stock selection, but run a lightweight `regime_monitor.py` daily. If the VIX/SMA gate triggers, use Alpaca to liquidate the book to cash (or hedge via SPY) the next morning. When the gate clears, re-enter the currently valid 63-day targets. This turns the gate from a slow-bear lottery ticket into actual daily risk management.

### 3. Attack Phase Variance via Inverse Volatility Sizing
You deferred position sizing, but equal-weighting 24 stocks is a massive driver of our phase-luck variance. A single high-beta tech stock blowing up disproportionately drives the -40.77% worst-case phase offsets. 

We don't need a fragile covariance matrix. Using the existing Polygon EOD data, we can implement a robust **Inverse Volatility weighting** (e.g., 63-day trailing variance). Equalizing risk contribution mathematically dampens the idiosyncratic portfolio shocks that make the 63-day offset phases so wildly divergent

### Opus 4.7
# Response: Gemini lands one solid hit, but mis-frames the others

## Where Gemini is right

**Universe-freeze contaminating the null — yes, conceded.** A permutation test on a biased universe gives a biased null. The fix is cheap on our stack: EDGAR has historical S&P 500 constituent filings, and Polygon's delisting-inclusive history lets us reconstruct the eligible set at each rebalance date. **This should run before the permutation test**, not in parallel. Good catch — order of operations matters.

## Where I push back

**On daily regime monitoring with intra-rebalance liquidation:** this is *adding a new strategy mid-OOS-validation*, exactly what the dossier warns against. You've introduced two new free parameters (daily-gate threshold, re-entry rule) that will be tuned on the same 2024-26 window that birthed the +9.26%. We'll get a beautiful backtest and learn nothing. If we do this, it must be **pre-registered with phase-averaged evaluation on a held-out window** (e.g., 2021-2023 on the $79 tier), not bolted on and re-fit. Otherwise we're laundering overfitting through a "risk management" label.

**On inverse-vol sizing as a phase-variance fix:** this conflates two different variances. Phase variance comes from *which rebalance dates you land on* — i.e., what stocks are eligible/selected at offset 0 vs offset 7. Inverse-vol weighting changes intra-portfolio risk contribution at a *given* rebalance; it doesn't change the fact that offset-0 selected NVDA pre-rip and offset-3 didn't. You'd dampen idiosyncratic blowups (real, modest benefit), but the -40.77% worst phase is almost certainly *selection* drift across the 63-day grid, not one stock detonating. **Test the decomposition first**: re-run the phase sweep with equal-weight vs inverse-vol — if phase spread barely tightens, sizing isn't the lever.

## What you missed

**The 63-day cadence itself is the unmeasured variable.** Every phase-luck result is conditional on n=63. We have no idea whether a 21-day or 126-day cadence has a tighter phase envelope on the same factors. This is one snapshot rebuild + a cadence sweep — and it would tell us whether the *shape* of the strategy is right before we tune sizing or gates. If 21-day rebalancing collapses phase variance to ±5pp, the whole debate changes; if it widens it, we know 63d is locally optimal and stop second-guessing.

**Priority stack:** (1) universe-freeze fix → (2) value-EPS fix → (3) cadence sweep + permutation null → (4) *then* argue about sizing and gates with a trustworthy measurement apparatus. Everything else is premature.

### Gemini
# Lead: The "Cadence Sweep" is a fatal multiple-comparisons trap

Opus, your proposed cadence sweep (testing 21d vs 63d vs 126d) to collapse phase variance is textbook overfitting. Sweeping rebalance frequencies on the same 2024-20

### Opus 4.7
# Closing: What we agreed on, what we didn't, and what to do Monday

## Agreement (banked)

1. **The measurement apparatus is broken and is the binding constraint.** Phase-luck envelope (±20-30pp) swamps the +9.26% headline; any improvement evaluated on offset-0 single runs is theater. Phase-distribution (median, IQR, %-positive, worst) is the only honest headline metric.
2. **Universe-freeze contaminates everything** — including any permutation null. Must be fixed first, using EDGAR historical constituents + Polygon delisting-inclusive history. Order of operations matters: biased universe → biased null → biased decisions.
3. **`alpha_vs_spy_pct` must die.** Replace with Jensen's α + Sharpe + max-DD triplet. The top-15 β-inflation episode (0.694→0.884) is proof it actively misleads.
4. **Value-EPS quarterly/YTD bug is a free win** — not because value will work, but because it's a known confound polluting composite attribution.

## Remaining disagreement

- **Daily regime monitoring (Gemini):** I still hold that introducing daily-gate threshold + re-entry rule mid-OOS-validation is exactly the multiple-comparisons failure the dossier warns against — *unless* pre-registered and evaluated phase-averaged on a held-out window (e.g., 2021-23 on $79 tier).
- **Cadence sweep (mine):** Gemini's cutoff response flagged it as overfitting. Fair concern, but the rebuttal: a cadence sweep evaluated *phase-averaged with a permutation null* is a measurement-characterization experiment, not a parameter search. The deliverable isn't "pick the best n" — it's "does phase variance scale with n?" If yes, 63d is structurally fragile and the whole shape is wrong; if no, we stop blaming cadence. That's diagnostic, not tuning. But I concede: only worth running *after* universe-freeze + Jensen's α are in.
- **Inverse-vol sizing:** Gemini overstates the phase-variance fix; I understated the idiosyncratic-blowup benefit. Truth is empirical — decompose first.

## Top 3 prioritized actions

**1. Rebuild the measurement layer (2-3 weeks, blocks everything).**
   - Dynamic PIT S&P 500 constituent resolution per rebalance (EDGAR filings + Polygon delistings).
   - Replace `alpha_vs_spy_pct` with Jensen's α / Sharpe / max-DD triplet across all reports.
   - Phase-distribution as the *only* headline. Permutation null (200x within-date shuffle) as the noise floor. **Decision rule: no config change ships unless median-phase Jensen's α clears the 95th percentile of the permutation null on a clean universe.**

**2. Fix the value-EPS period-disambiguation bug (2 days).** Removes a known confound from composite attribution. Re-run m/q vs m/q/v with the new measurement layer — only then do we know if value adds anything.

**3. Pre-register ONE structural experiment on the held-out 2021-23 window** ($79 tier required): either the cadence-characterization sweep *or* the daily-regime-gate decoupling — not both, not on 2024-26. Pre-commit the decision rule before running. Everything else (inverse-vol, sector caps, top-N) waits until the apparatus says we can hear signal over noise.

The forward paper run (started 2026-05-27, review 2026-08-27) is the un-overfittable check on all of this. Don't touch the live config until then. Build the measurement layer in the meantime — that's the only work that compounds.

### Gemini
**Lead: We are in violent agreement on the prime directive — the current measurement apparatus generates illusions, not alpha.** Until the ±20-30pp phase-noise envelope is neutralized and the universe-freeze bias is eliminated, we aren't doing quantitative finance; we are doing astrology. 

I fully concede to your Out-Of-Sample (OOS) discipline. You are absolutely right that introducing a daily regime gate or a cadence sweep on the same 2024-2026 discovery window is a multiple-comparisons trap. 

However, you underestimate *why* inverse-volatility sizing is structurally necessary *for* your permutation test. Equal-weighting a concentrated 24-stock portfolio maximizes idiosyncratic noise, which artificially inflates the variance of your null distribution. If you don't mathematically suppress individual stock blowups using Polygon's EOD trailing variance, your permutation hurdle rate becomes impossibly high. It is a measurement stabilizer, not just a sizing tweak.

Here is the final, synthesized action plan for Monday. 

### Top 3 Prioritized Actions

**1. Eradicate the Confounders (Weeks 1-2)**
Before running another backtest, fix the data leaks. 
*   **Universe:** Rebuild `scripts/build_snapshot.py` to dynamically resolve the S&P 500 constituents point-in-time per rebalance using EDGAR historical filings and Polygon delisting data. 
*   **Fundamentals:** Fix the EDGAR EPS quarterly/YTD period-mixing bug.
*   **Metrics:** Strip `alpha_vs_spy_pct` from the codebase. Hardcode the Jensen's α, Sharpe, and Max-DD triplet into the reporting layer.

**2. Establish the True Noise Floor (Week 3)**
With a clean universe, run your 200x permutation shuffle across all 9 offsets. The new benchmark for shipping *any* model update is that its phase-median Jensen's α must clear the 95th percentile of this
